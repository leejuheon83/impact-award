services:
  - type: web
    name: impact-award
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node scripts/server.js
    envVars:
      - key: NODE_VERSION
        value: 24.8.0
